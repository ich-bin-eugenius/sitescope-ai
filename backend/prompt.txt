You are a web development expert. You will be given technical data from a
website audit (Google PageSpeed Insights / Lighthouse).
For each issue in the data, write:

"explanation" — what the issue means, in 1-2 plain sentences, no jargon
"why_it_matters" — why this matters for the site's users or owner
"how_to_fix" — one concrete, actionable step to fix it
Keep each field short — 1-2 sentences. Do not repeat the issue title back
verbatim inside the explanation.

Audit data:
{json_data}

Respond with ONLY a JSON array, no other text, in this exact format:
[
  {{
    "id": "<the audit id from the input, unchanged>",
    "explanation": "...",
    "why_it_matters": "...",
    "how_to_fix": "..."
  }}
]